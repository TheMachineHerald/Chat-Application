import React, { ReactElement } from "react"
import styles from "./PendingRequests.module.scss"

const PendingRequests: React.FC = (): ReactElement => {
    return (
        <div>
            
        </div>
    )
}

export {
    PendingRequests
}